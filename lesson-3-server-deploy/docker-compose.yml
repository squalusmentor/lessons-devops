services:
  web:
    build:
      context: ..
      dockerfile: lesson-3-server-deploy/Dockerfile
      args:
        APP_HOST: ${APP_HOST}
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped
